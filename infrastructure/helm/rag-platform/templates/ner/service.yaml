apiVersion: v1
kind: Service
metadata:
  name: {{ include "rag-platform.fullname" . }}-ner
  labels:
    {{- include "rag-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: ner-service
spec:
  type: {{ .Values.ner.service.type }}
  ports:
    - port: {{ .Values.ner.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rag-platform.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: ner-service
